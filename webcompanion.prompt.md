
Can you create an angular 19 application named RealesateWebCompanion that serves as a web companion for real estate agents, providing them with tools and resources to manage their listings, clients, and appointments effectively? The application should include features such as a dashboard for tracking listings and appointments, a client management system, and integration with popular real estate platforms for seamless listing updates.

The data in json format needs to be displayed in the app and users should be able to interact with it within grid with every column being sortable and filterable. The data is stored in json files and the app should read from those files to display the information.

listings found in appartments.jso file

areas found in areas.json file

toogles for features are found in toggles.json file

unit types found in unitTypes.json file

samples json files are found in C:\local\angulardev\montreal4rent\website\src\assets\data

When users save information data is saved in the browser 

The app will be deployed on github pages and the code will be available in a public repository on github.

Add an option Export (download) data in JSON format to google drive folder

Add an option Import (upload) data from google drive folder in JSON format
